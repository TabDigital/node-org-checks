{
  "name": "org-checks",
  "version": "1.0.0",
  "description": "Perform security checks on github",
  "private": true,
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && tape test/*"
  },
  "repository": "TabDigital/org-checks",
  "keywords": [
    "check",
    "security",
    "github"
  ],
  "license": "MIT",
  "dependencies": {
    "ghutils": "^3.2.0",
    "map-limit": "0.0.1"
  },
  "devDependencies": {
    "dependency-check": "^2.5.1",
    "istanbul": "^0.4.1",
    "standard": "^5.4.1",
    "tape": "^4.4.0"
  },
  "files": [
    "index.js",
    "bin/*"
  ]
}
